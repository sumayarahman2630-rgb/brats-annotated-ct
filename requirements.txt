torch>=2.1
numpy>=1.24
SimpleITK>=2.3
PyYAML>=6.0
tqdm>=4.66
tensorboard>=2.15
scikit-image>=0.22
scipy>=1.11   # Stage 3 post-processing: connected-component filtering (inference/postprocessing.py) -- was
              # already present transitively via scikit-image, listed explicitly since it's now imported directly
matplotlib>=3.8
pydicom>=2.4   # Stage 3: reads the Jordan hospital CT/mask DICOM export (RGB, secondary-capture)

# dev/test only, not needed on Kaggle for training/inference:
pytest>=7.4
